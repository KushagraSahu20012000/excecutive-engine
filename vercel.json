{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "if [ -f client/package.json ]; then npm --prefix client run build && rm -rf dist && mkdir -p dist && cp -R client/dist/. dist/; else npm run build; fi",
  "outputDirectory": "dist",
  "functions": {
    "api/[...route].js": {
      "runtime": "nodejs22.x",
      "maxDuration": 10
    }
  },
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}